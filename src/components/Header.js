import { StyledHeader } from "./styled/Header.styled";

export const Header = () => {
    return (
        <StyledHeader>
            <h1>Head</h1>

        </StyledHeader>
    );
};

export default Header;